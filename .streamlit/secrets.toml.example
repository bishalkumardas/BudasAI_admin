# Copy this file to .streamlit/secrets.toml. Do not commit the real file.
SUPABASE_URL = "https://your-project.supabase.co"
# For an admin dashboard, use the *service_role* key, kept only on the server.
# The publishable/anon key only works if your RLS policies allow these operations.
SUPABASE_KEY = "your-service-role-key"
